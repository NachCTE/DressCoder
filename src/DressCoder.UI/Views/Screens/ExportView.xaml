<UserControl x:Class="DressCoder.UI.Views.Screens.ExportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:DressCoder.UI.ViewModels.Screens"
             d:DataContext="{d:DesignInstance Type=vm:ExportViewModel}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Exportación" FontSize="22" FontWeight="Bold" Margin="0,0,0,16" />

        <Grid Grid.Row="1" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" Text="{Binding OutputDirectory, UpdateSourceTrigger=PropertyChanged}" Padding="6" />
            <Button Grid.Column="1" Content="Elegir carpeta..." Margin="8,0,0,0" Padding="10,6" Click="OnBrowseOutputClick" />
            <Button Grid.Column="2" Content="Exportar" Command="{Binding ExportCommand}" Margin="8,0,0,0" Padding="10,6" />
        </Grid>

        <ProgressBar Grid.Row="2" IsIndeterminate="True" Height="4" Margin="0,0,0,12"
                     Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibilityConverter}}" />

        <TextBlock Grid.Row="3" Text="{Binding StatusMessage}" TextWrapping="Wrap" Margin="0,0,0,12" />

        <ListBox Grid.Row="4" ItemsSource="{Binding ValidationMessages}" />
    </Grid>
</UserControl>
