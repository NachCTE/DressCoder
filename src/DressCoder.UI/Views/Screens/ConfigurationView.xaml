<UserControl x:Class="DressCoder.UI.Views.Screens.ConfigurationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:DressCoder.UI.ViewModels.Screens"
             d:DataContext="{d:DesignInstance Type=vm:ConfigurationViewModel}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <StackPanel Margin="24" MaxWidth="480" HorizontalAlignment="Left">
        <TextBlock Text="Configuración" FontSize="22" FontWeight="Bold" Margin="0,0,0,16" />

        <TextBlock Text="Nombre del plugin (sin espacios, ej: AerithBahamutRobe)" Margin="0,0,0,4" />
        <TextBox Text="{Binding PluginName, UpdateSourceTrigger=PropertyChanged}" Padding="6" Margin="0,0,0,12" />

        <TextBlock Text="Nombre visible" Margin="0,0,0,4" />
        <TextBox Text="{Binding FriendlyName, UpdateSourceTrigger=PropertyChanged}" Padding="6" Margin="0,0,0,12" />

        <TextBlock Text="Descripción" Margin="0,0,0,4" />
        <TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" Padding="6" Margin="0,0,0,12"
                 TextWrapping="Wrap" AcceptsReturn="True" Height="60" />

        <TextBlock Text="Autor" Margin="0,0,0,4" />
        <TextBox Text="{Binding CreatedBy, UpdateSourceTrigger=PropertyChanged}" Padding="6" Margin="0,0,0,12" />

        <TextBlock Text="Versión" Margin="0,0,0,4" />
        <TextBox Text="{Binding VersionName, UpdateSourceTrigger=PropertyChanged}" Padding="6" Margin="0,0,0,12" />

        <TextBlock Text="Ícono (PNG, opcional; si no se elige uno se usa el ícono por defecto)" Margin="0,0,0,4" />
        <Grid Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" Text="{Binding IconPath, UpdateSourceTrigger=PropertyChanged}" Padding="6" IsReadOnly="True" />
            <Button Grid.Column="1" Content="Elegir..." Margin="8,0,0,0" Padding="10,6" Click="OnBrowseIconClick" />
        </Grid>

        <Button Content="Guardar configuración" Command="{Binding SaveCommand}" Padding="10,6" HorizontalAlignment="Left" Margin="0,4,0,12" />

        <TextBlock Text="{Binding StatusMessage}" TextWrapping="Wrap" Foreground="Gray" />
    </StackPanel>
</UserControl>
