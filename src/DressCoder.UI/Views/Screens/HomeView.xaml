<UserControl x:Class="DressCoder.UI.Views.Screens.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:DressCoder.UI.ViewModels.Screens"
             d:DataContext="{d:DesignInstance Type=vm:HomeViewModel}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <StackPanel Margin="24">
        <TextBlock Text="Bienvenido" FontSize="22" FontWeight="Bold" Margin="0,0,0,4" />
        <TextBlock Text="Convertí un mod replacer de FF7 Rebirth a un mod Dresscode en unos pocos pasos."
                   Foreground="Gray" Margin="0,0,0,20" TextWrapping="Wrap" />

        <Border Background="#1E1E1E" CornerRadius="6" Padding="16" Margin="0,0,0,16">
            <StackPanel>
                <TextBlock Text="Estado del entorno" FontWeight="Bold" Foreground="White" Margin="0,0,0,8" />
                <TextBlock Text="{Binding ToolsStatusMessage}" TextWrapping="Wrap" Foreground="White" />
                <Button Content="Volver a verificar" Command="{Binding RefreshDiagnosticsCommand}"
                        HorizontalAlignment="Left" Margin="0,10,0,0" Padding="8,4" />
            </StackPanel>
        </Border>

        <Button Content="Importar un mod →" Command="{Binding GoToImportCommand}"
                HorizontalAlignment="Left" Padding="12,8" FontSize="14" />
    </StackPanel>
</UserControl>
