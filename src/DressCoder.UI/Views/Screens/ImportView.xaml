<UserControl x:Class="DressCoder.UI.Views.Screens.ImportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:DressCoder.UI.ViewModels.Screens"
             d:DataContext="{d:DesignInstance Type=vm:ImportViewModel}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Importar Mod" FontSize="22" FontWeight="Bold" Margin="0,0,0,4" />
        <TextBlock Grid.Row="0" Text="Elegí el archivo .pak/.utoc/.ucas del replacer, o la carpeta que los contiene."
                   Foreground="Gray" Margin="0,32,0,16" TextWrapping="Wrap" />

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,12">
            <Button Content="Elegir archivo..." Click="OnPickFileClick" Padding="10,6" Margin="0,0,8,0" />
            <Button Content="Elegir carpeta..." Click="OnPickFolderClick" Padding="10,6" />
            <ProgressBar IsIndeterminate="True" Width="120" Margin="16,0,0,0"
                         Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibilityConverter}}" />
        </StackPanel>

        <TextBlock Grid.Row="2" Text="{Binding StatusMessage}" TextWrapping="Wrap" Margin="0,0,0,12" />

        <ListBox Grid.Row="3" ItemsSource="{Binding DetectedAssetPaths}" FontFamily="Consolas" FontSize="12" />
    </Grid>
</UserControl>
